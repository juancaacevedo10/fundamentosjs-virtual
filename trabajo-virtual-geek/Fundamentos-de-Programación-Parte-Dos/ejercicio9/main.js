const name = 'juan david torres';
const contenedor = document.querySelector(".contenedor");

contenedor.innerHTML = `<h2>El nombre de mi compañero es ${name}, y está compuesto por ${name.length} caracteres</h2>`
